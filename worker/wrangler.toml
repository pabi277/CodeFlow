name = "codeflow-oauth-proxy"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Routes: adjust to your domain
routes = [
  { pattern = "auth.codeflow.app/exchange", zone_name = "codeflow.app" },
]

# Set these as secrets (wrangler secret put):
#   GITHUB_CLIENT_ID
#   GITHUB_CLIENT_SECRET
