# Browser-visible OAuth client ID. Safe to expose in the frontend bundle.
VITE_GITHUB_CLIENT_ID=

# Server-only values for api/exchange.js. Do not prefix these with VITE_.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
